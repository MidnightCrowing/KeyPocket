<?xml version="1.0" encoding="utf-8" ?>

<Window
    Title="KeyPocket"
    mc:Ignorable="d"
    x:Class="KeyPocket.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            BackRequested="TitleBar_OnBackRequested"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="TitleBar_OnPaneToggleRequested"
            Title="KeyPocket"
            x:Name="titleBar">

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/KeyPocket.png" />
            </TitleBar.IconSource>

            <TitleBar.Content>
                <AutoSuggestBox
                    PlaceholderText="Search.."
                    QueryIcon="Find"
                    VerticalAlignment="Center"
                    Width="360" />
            </TitleBar.Content>
        </TitleBar>

        <NavigationView
            AlwaysShowHeader="False"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="True"
            SelectionChanged="NavigationView_SelectionChanged"
            x:Name="navView">

            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="Home"
                    Tag="HomePage"
                    x:Name="homePage">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Home" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemHeader Content="Providers" />

                <NavigationViewItem
                    Content="Menu Item2"
                    SelectsOnInvoked="True"
                    Tag="SamplePage2"
                    x:Name="samplePage2Item">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Save" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    Content="Menu Item3"
                    Tag="SamplePage3"
                    x:Name="samplePage3Item">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Refresh" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="contentFrame" />
        </NavigationView>
    </Grid>
</Window>